[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  CI = "false" # Prevent warnings from failing the build
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
